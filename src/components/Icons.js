"use client";

import styled from "@emotion/styled";

import { LuSearch } from "react-icons/lu";

export const StyledSearchIcon = styled(LuSearch)`
  position: absolute;
  right: 0;
  top: 50%;
  transform: translateY(-50%);
  margin: 0 5px;
  color: #000;
  font-size: 20px;
`;
